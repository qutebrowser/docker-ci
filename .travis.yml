sudo: required
services: docker
language: generic

env:
  matrix:
    - IMAGE=archlinux
    - IMAGE=debian-jessie
    - IMAGE=ubuntu-xenial

script:
  - docker build -t "qutebrowser/travis:$IMAGE" -f "$IMAGE/Dockerfile"
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker push "qutebrowser/travis:$IMAGE"

notifications:
  irc:
    channels:
      - "chat.freenode.net#qutebrowser-dev"
    on_success: always
    on_failure: always
    skip_join: true
    template:
      - "%{repository}#%{build_number} (%{branch} - %{commit} : %{author}): %{message}"
      - "%{compare_url} - %{build_url}"
